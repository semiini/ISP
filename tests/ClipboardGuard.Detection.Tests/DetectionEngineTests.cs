using Xunit;

namespace ClipboardGuard.Detection.Tests;

/// <summary>
/// Placeholder test class — replace with real tests in Task 3.
/// </summary>
public class DetectionEngineTests
{
    [Fact]
    public void Placeholder_ShouldPass()
    {
        // TODO: Implement tests when Task 3 (Sensitive Data Detection Engine) is complete.
        // Expected coverage for each SensitiveDataType:
        //  - At least one true-positive sample (should detect)
        //  - At least one true-negative sample (should not detect)
        //  - No raw matched text stored in DetectionResult.Matches
        Assert.True(true);
    }
}
